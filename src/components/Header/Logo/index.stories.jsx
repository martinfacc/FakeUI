import React from 'react'
import Logo from './index'

export default {
	title: 'Logo',
	component: Logo,
}

const Template = () => <Logo />

export const Default = Template.bind({})
Default.args = {}
